# linked lists
: X Input, X Output, X Next Editor, X Representation Text, X Other Buttons *use destroy(), X tooltip
: New Link when no links
: Buttons Autho Shift
: write efficient code
# dijkstra
# disjoint sets
# circular array